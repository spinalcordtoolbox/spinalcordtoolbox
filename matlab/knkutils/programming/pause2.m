function f = pause2(n)

% function f = pause2(n)
%
% <n> is the number of seconds
%
% just like pause.m except that we return a value, which is always 0.
% this function is useful in special programming contexts.
%
% example:
% pause2(1)

pause(n);
f = 0;
